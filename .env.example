# If empty, the app will fall back to the local mock dataset at src/mocks/users.json
# To use a real backend, set this to your API base URL (no trailing slash)
VITE_API_URL=

# App display name
VITE_APP_NAME=UsersSearch

# Optional: change dev port (Vite will use its server config if set)
# VITE_DEV_PORT=5173
